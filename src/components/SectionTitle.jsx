import React from 'react'

const SectionTitle = ({ title }) => (
    <div className='flex justify-center items-center'>
        <p className='title gradient-ink-free-text text-[2rem] font-bold'>{title}</p>
    </div>
)

export default SectionTitle