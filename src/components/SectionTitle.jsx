import React from 'react'

const SectionTitle = ({ title }) => (
    <div className='flex justify-center items-center'>
        <p className='title gradient-text-180 text-[2rem]'>{title}</p>
    </div>
)

export default SectionTitle